{
  "name": "check-binary-file",
  "version": "1.0.0",
  "description": "check if a file is binary or not",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "isbinaryfile": "^4.0.6"
  }
}
